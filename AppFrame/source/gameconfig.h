// このファイルは Python により自動生成されました。直接編集しないでください。
#pragma once

const constexpr auto SCREEN_WIDTH = 1280;
const constexpr auto SCREEN_HEIGHT = 720;
const constexpr auto FADE_FRAME = 60;
const constexpr auto FADE_WAIT = 60;
const constexpr auto FADE_OUT_WAIT = 30;
